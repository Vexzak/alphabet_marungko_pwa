# TODO

- [ ] Reorder game phases in `client/src/pages/Home.tsx` so after look & circle comes `AssessCanvas` first, then (Next) `Assessment` (drag images).
- [ ] Update `handleBack()` mapping in `client/src/pages/Home.tsx` to match the new phase order.
- [ ] Quick manual test: finish look & circle → verify canvas tracing runs; click Next → verify drag assessment runs.

